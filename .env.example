# ==================== LDAP 基本配置 ====================
# LDAP 域名配置
LDAP_DOMAIN=example.com
LDAP_ROOT=dc=example,dc=com

# LDAP 管理员账号
LDAP_ADMIN_USERNAME=admin
LDAP_ADMIN_PASSWORD=ChangeMe123!

# LDAP 配置管理员账号
LDAP_CONFIG_ADMIN_USERNAME=config_admin
LDAP_CONFIG_ADMIN_PASSWORD=ChangeMe456!

# LDAP 访问日志管理员账号
LDAP_ACCESSLOG_ADMIN_USERNAME=log_admin
LDAP_ACCESSLOG_ADMIN_PASSWORD=ChangeMe789!

# ==================== LDAP 端口配置 ====================

# 主机暴露端口（通过 Nginx）
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
NGINX_LDAP_PORT=389
NGINX_LDAPS_PORT=636

# ==================== TLS 配置 ====================
LDAP_ENABLE_TLS=yes
LDAP_REQUIRE_TLS=no

# ==================== LDAP Account Manager 配置 ====================
# LAM 管理员密码（用于登录 LAM 配置界面）
LAM_PASSWORD=lam

# ==================== 复制（Replication）配置 ====================
# 是否启用多主复制（yes=启用，no=禁用）
# 启用后，所有节点都可读写，数据自动同步
ENABLE_REPLICATION=yes

# 当前节点 ID（1-999，每个节点必须唯一）
# 部署到不同地区时，为每个节点分配不同的 ID
SERVER_ID=1

# 其他复制节点地址（逗号分隔，格式：ldap://host:port）
# 示例：ldap://node2.example.com:389,ldap://node3.example.com:389
# 留空表示单节点模式
REPLICATION_HOSTS=

# 复制用户配置（用于节点间同步）
# 用户名（默认：replica_admin）
REPLICATION_USER=replica_admin

# 复制用户密码
REPLICATION_PASSWORD=ChangeMe123!

# ==================== 调试配置 ====================
BITNAMI_DEBUG=false
